export type Booking = {
    name: string,
    minute: string,
    yellow_card: boolean,
    red_card: boolean,
    second_yellow_card: boolean,
    sending_off: boolean,
    home_team: boolean,
    away_team: boolean,
};